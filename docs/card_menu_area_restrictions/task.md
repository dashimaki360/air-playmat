# タスクリスト: カードメニューのエリア別出し分け実装

- [ ] 型定義の更新 (`src/types/game.ts`)
  - (現時点では追加・変更なし)
- [x] UIコンポーネント（カードメニュー）の改修
  - [x] `src/components/CardMenu.tsx` の Props に `area: AreaId` を追加する。
  - [x] `area === 'bench'` のとき、状態異常系のUIが表示されないようにガードを追加する。
- [x] UIコンポーネント（カード本体）の改修
  - [x] `CardMenu` コンポーネントへ `area={area}` を渡すように修正。
- [x] 状態管理による制約（システム挙動の自動化）
  - [x] `src/components/Board.tsx` の `handleDragEnd` 内部で、`bench` に 이동したカードの `cnd` を空配列(`[]`)にリセットする処理を追加する。
- [x] 動作確認（ローカル）
  - [x] ベンチのカードをクリックしたとき、状態異常のボタンが出ないか確認。
  - [x] 状態異常のカードをベンチに戻した際、アイコン（状態）がリセットされるか確認。
